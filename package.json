{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-highlight": "webpack",
    "audit": "npm audit --audit-level=high",
    "audit:critical": "npm audit --audit-level=critical",
    "audit:fix": "npm audit fix",
    "audit:check": "npm audit --audit-level=high || (echo 'ERROR: Vulnerabilities found. Run npm audit:fix to attempt automatic fixes, or review manually.' && exit 1)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "highlight.js": "^11.11.1"
  },
  "devDependencies": {
    "shx": "^0.4.0",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1"
  },
  "overrides": {
    "glob": "^10.0.0"
  }
}
